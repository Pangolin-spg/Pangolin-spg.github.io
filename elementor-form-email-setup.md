# Elementor 表单邮件配置完整指南

## 🎯 问题描述

**现象：**
- ✅ WPForms 表单提交后能收到邮件
- ❌ Elementor 表单提交后收不到邮件
- ✅ 提交记录在 Elementor → Submissions 中可以看到

**原因：**
Elementor 表单需要手动配置 "Email" 动作才能发送邮件。

---

## 🔧 解决方案

### 方案 A：配置 Elementor 表单的 Email 动作（推荐）

#### 步骤 1：编辑表单

1. **进入编辑模式**
   ```
   WordPress 后台 → 页面 → 找到包含表单的页面
   点击 "使用 Elementor 编辑"
   ```

2. **选择表单小部件**
   ```
   点击表单
   左侧面板会显示表单设置
   ```

#### 步骤 2：启用 Email 动作

1. **找到 "Actions After Submit"**
   ```
   左侧面板 → Form → Actions After Submit
   ```

2. **勾选 "Email"**
   ```
   如果没有勾选，点击勾选框启用
   ```

3. **配置 Email 设置**
   ```
   点击 "Email" 展开设置
   ```

#### 步骤 3：填写邮件配置

**基础设置：**

```
Email To: 
  your-email@example.com
  （改成您的真实邮箱，多个邮箱用逗号分隔）

Email Subject:
  新的表单提交 - [field id="name"]
  （可以使用字段动态标签）

From Email:
  noreply@pangolinfo.com
  （使用您的域名邮箱，不要用 Gmail 等）

From Name:
  Pangolin 官网
  （发件人名称）

Reply To:
  [field id="email"]
  （使用提交者的邮箱，方便回复）
```

**邮件内容：**

```
Message:
  [all-fields]
  
  或自定义格式：
  
  姓名：[field id="name"]
  邮箱：[field id="email"]
  主题：[field id="subject"]
  消息：[field id="message"]
  
  提交时间：[date]
  提交 IP：[remote_ip]
```

**高级设置（可选）：**

```
Email Content Type: HTML
  （如果想要 HTML 格式的邮件）

Email To 2:
  另一个接收邮箱（可选）

Email Subject 2:
  第二封邮件的主题（可选）
```

#### 步骤 4：保存并测试

1. **保存页面**
   ```
   点击左下角 "更新" 或 "发布"
   ```

2. **测试表单**
   ```
   访问前台页面
   填写表单并提交
   检查邮箱（包括垃圾邮件箱）
   ```

---

### 方案 B：使用代码自动配置（高级）

如果您有多个 Elementor 表单，可以使用代码统一配置邮件发送。

**在 `functions.php` 中添加：**

```php
/**
 * Elementor 表单自动发送邮件
 * 如果表单没有配置 Email 动作，自动发送到管理员邮箱
 */
add_action('elementor_pro/forms/new_record', 'auto_send_elementor_form_email', 10, 2);
function auto_send_elementor_form_email($record, $handler) {
    // 获取表单名称
    $form_name = $record->get_form_settings('form_name');
    
    // 获取所有字段
    $raw_fields = $record->get('fields');
    $fields = [];
    foreach ($raw_fields as $id => $field) {
        $fields[$id] = $field['value'];
    }
    
    // 构建邮件内容
    $message = "表单名称：" . $form_name . "\n\n";
    $message .= "提交时间：" . current_time('Y-m-d H:i:s') . "\n\n";
    $message .= "表单内容：\n";
    $message .= "-------------------\n";
    
    foreach ($raw_fields as $id => $field) {
        $message .= $field['title'] . ": " . $field['value'] . "\n";
    }
    
    $message .= "-------------------\n";
    $message .= "提交 IP: " . $_SERVER['REMOTE_ADDR'] . "\n";
    
    // 发送邮件
    $to = get_option('admin_email'); // 发送到管理员邮箱
    $subject = '新的表单提交 - ' . $form_name;
    $headers = array(
        'Content-Type: text/plain; charset=UTF-8',
        'From: Pangolin 官网 <noreply@pangolinfo.com>'
    );
    
    wp_mail($to, $subject, $message, $headers);
    
    // 记录日志
    error_log('[Elementor Form] 邮件已发送 - 表单: ' . $form_name . ' - 收件人: ' . $to);
}
```

**注意：** 这个代码会给所有 Elementor 表单发送邮件，即使表单本身已经配置了 Email 动作。

---

## 🔍 故障排查

### 问题 1：配置了 Email 动作但还是收不到邮件

**可能原因：**
1. WordPress 邮件发送功能有问题
2. 邮件被服务器拦截
3. 邮件进入垃圾箱
4. From Email 使用了外部邮箱（如 Gmail）

**解决方法：**

#### 方法 1：安装邮件日志插件

```
WordPress 后台 → 插件 → 安装插件
搜索："WP Mail Logging"
安装并启用

提交表单后查看：
WordPress 后台 → WP Mail Log
查看邮件是否发送成功
```

#### 方法 2：测试 WordPress 邮件功能

```
WordPress 后台 → 工具 → 站点健康 → 信息 → 邮件
或安装 "Check Email" 插件测试邮件发送
```

#### 方法 3：配置 SMTP

如果 WordPress 默认邮件发送不稳定，配置 SMTP：

```
参考之前的文档：aliyun-directmail-setup-guide.md
或使用 WP Mail SMTP 插件
```

### 问题 2：邮件发送成功但收不到

**检查清单：**
- [ ] 检查垃圾邮件箱
- [ ] 检查邮件过滤规则
- [ ] 检查邮箱容量是否已满
- [ ] 尝试发送到其他邮箱测试

### 问题 3：From Email 导致邮件被拒

**问题：**
```
From Email: user@gmail.com  ← 错误！
```

**解决：**
```
From Email: noreply@pangolinfo.com  ← 正确！
使用您自己域名的邮箱
```

### 问题 4：邮件内容为空

**检查：**
```
Message 字段是否填写了 [all-fields] 或具体字段
字段 ID 是否正确（在表单字段设置中查看）
```

---

## 📊 推荐配置

### 配置 1：简单版（推荐新手）

```
Email To: your-email@example.com
Email Subject: 新的表单提交
From Email: noreply@pangolinfo.com
From Name: Pangolin 官网
Message: [all-fields]
```

### 配置 2：专业版（推荐）

```
Email To: sales@pangolinfo.com, support@pangolinfo.com
Email Subject: 新客户咨询 - [field id="name"]
From Email: noreply@pangolinfo.com
From Name: Pangolin 官网表单系统
Reply To: [field id="email"]
Message:
  收到新的客户咨询！
  
  客户信息：
  姓名：[field id="name"]
  邮箱：[field id="email"]
  公司：[field id="company"]
  
  咨询内容：
  [field id="message"]
  
  ---
  提交时间：[date] [time]
  来源页面：[page_url]
  客户 IP：[remote_ip]
```

### 配置 3：多语言版

**中文表单：**
```
Email Subject: 新的表单提交 - [field id="name"]
Message: [all-fields]
```

**英文表单：**
```
Email Subject: New Form Submission - [field id="name"]
Message: [all-fields]
```

---

## 🎯 最佳实践

### 1. 使用域名邮箱作为发件人

```
✅ 正确：noreply@pangolinfo.com
❌ 错误：youremail@gmail.com
```

### 2. 设置 Reply To 为提交者邮箱

```
Reply To: [field id="email"]
这样您可以直接回复邮件给客户
```

### 3. 在邮件主题中包含关键信息

```
Email Subject: 新客户咨询 - [field id="name"] - [field id="company"]
方便在邮箱中快速识别
```

### 4. 使用 HTML 格式美化邮件

```
Email Content Type: HTML

Message:
<h2>新的表单提交</h2>
<table>
  <tr>
    <td><strong>姓名：</strong></td>
    <td>[field id="name"]</td>
  </tr>
  <tr>
    <td><strong>邮箱：</strong></td>
    <td>[field id="email"]</td>
  </tr>
</table>
```

### 5. 发送确认邮件给客户

添加第二个 Email 动作：

```
Email 2 设置：
Email To: [field id="email"]  ← 发送给提交者
Email Subject: 感谢您的咨询
Message:
  您好 [field id="name"]，
  
  感谢您联系 Pangolin！
  我们已收到您的咨询，将在 24 小时内回复。
  
  您的咨询内容：
  [field id="message"]
  
  ---
  Pangolin 团队
```

---

## 🔔 通知方式对比

| 方式 | 优点 | 缺点 | 推荐度 |
|------|------|------|--------|
| **Elementor Email** | 配置简单，功能完整 | 依赖 WordPress 邮件 | ⭐⭐⭐⭐⭐ |
| **WP Mail SMTP** | 稳定可靠 | 需要配置 SMTP | ⭐⭐⭐⭐⭐ |
| **Webhook** | 灵活强大 | 需要编程知识 | ⭐⭐⭐ |
| **Zapier** | 功能丰富 | 有费用 | ⭐⭐⭐⭐ |
| **Submissions** | 不会丢失 | 需要登录后台查看 | ⭐⭐⭐ |

---

## 📝 快速检查清单

### ✅ Elementor 表单邮件配置检查

- [ ] Actions After Submit 中勾选了 "Email"
- [ ] Email To 填写了正确的邮箱地址
- [ ] Email Subject 已填写
- [ ] From Email 使用了域名邮箱（不是 Gmail 等）
- [ ] Message 填写了 [all-fields] 或具体字段
- [ ] 保存了页面
- [ ] 清除了缓存
- [ ] 测试提交表单
- [ ] 检查了邮箱（包括垃圾箱）
- [ ] 查看了 WP Mail Log（如果安装了）

---

## 🆘 还是收不到邮件？

### 请提供以下信息：

1. **Elementor 表单设置截图**
   - Actions After Submit 部分
   - Email 动作配置

2. **WP Mail Log 记录**
   - 邮件是否发送成功
   - 错误信息

3. **浏览器控制台**
   - 提交表单时是否有错误

4. **服务器信息**
   - 使用的主机商
   - 是否有邮件发送限制

---

## 💡 推荐方案

### 最佳组合：Elementor Email + WP Mail SMTP + 阿里云邮件推送

1. **配置 Elementor Email 动作** ✅
2. **安装 WP Mail SMTP 插件** ✅
3. **配置阿里云邮件推送** ✅（参考 aliyun-directmail-setup-guide.md）

这样可以确保：
- ✅ 邮件发送稳定可靠
- ✅ 送达率高（95%+）
- ✅ 不会被标记为垃圾邮件
- ✅ 有详细的发送日志

---

**需要我帮您检查具体的表单配置吗？** 😊
