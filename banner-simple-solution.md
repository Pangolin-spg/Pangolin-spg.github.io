# 横幅布局 - 最终最简方案

## ✅ 最终解决方案

### 核心思路

**只给 body 添加 padding-top，所有内容（页眉+背景）都向下移动**

```css
/* 最简方案 */
body.has-top-banner {
    padding-top: 40px !important;
}

/* sticky 导航栏在横幅下方 */
body.has-top-banner header.sticky {
    top: 40px !important;
}

/* 关闭横幅后，移除 padding */
body.banner-closed {
    padding-top: 0 !important;
}
```

---

## 🎨 布局效果

### 有横幅时
```
┌─────────────────────────────────────────────────┐
│ 横幅 (40px, fixed, top: 0, z-index: 999999)     │
├─────────────────────────────────────────────────┤ ← 横幅底部
│ body (padding-top: 40px)                        │
│ ┌─────────────────────────────────────────────┐ │
│ │ 导航栏（透明）                               │ │
│ ├─────────────────────────────────────────────┤ │
│ │ 页面内容                                     │ │
│ │ ┌─────────────────────────────────────────┐ │ │
│ │ │ 蓝色渐变背景                             │ │ │
│ │ └─────────────────────────────────────────┘ │ │
│ └─────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────┘
```

### 关闭横幅后
```
┌─────────────────────────────────────────────────┐
│ body (padding-top: 0)                           │
│ ┌─────────────────────────────────────────────┐ │
│ │ 导航栏（回到顶部）                           │ │
│ ├─────────────────────────────────────────────┤ │
│ │ 页面内容                                     │ │
│ └─────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────┘
```

---

## 📋 完整的 CSS

### 桌面端
```css
/* 横幅 */
.pangolin-promo-banner {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    width: 100%;
    height: 40px;
    background-color: #F8F8F8;
    z-index: 999999 !important;
}

/* body 向下移动 */
body.has-top-banner {
    padding-top: 40px !important;
}

/* sticky 导航栏在横幅下方 */
body.has-top-banner header.sticky {
    top: 40px !important;
    z-index: 99 !important;
}

/* 关闭横幅后 */
body.banner-closed {
    padding-top: 0 !important;
}
```

### 手机端
```css
@media (max-width: 768px) {
    .pangolin-promo-banner {
        height: 56px;
    }
    
    body.has-top-banner {
        padding-top: 56px !important;
    }
    
    body.has-top-banner header.sticky {
        top: 56px !important;
    }
}
```

---

## 🎯 为什么这是最简方案？

### 优点
1. ✅ **简单直接**：只需要一行 CSS
2. ✅ **所有内容向下移动**：页眉和背景一起移动
3. ✅ **自动恢复**：关闭横幅后，移除 `has-top-banner` class，padding 自动消失
4. ✅ **无需负 margin**：不需要复杂的抵消逻辑
5. ✅ **易于维护**：代码清晰，容易理解

### 为什么有效？
```
- body 的 padding-top 把所有内容向下推 40px
- 横幅是 fixed 定位，不受影响，保持在顶部
- 导航栏和页面内容都在 body 内，所以都向下移动
- 关闭横幅后，移除 has-top-banner class，padding 消失，内容回到顶部
```

---

## 🧪 测试步骤

### 步骤 1：清除缓存
```
Ctrl+F5 (Windows) 或 Cmd+Shift+R (Mac)
```

### 步骤 2：访问英文首页

**检查点：**
```
✅ 横幅在最顶部
✅ 导航栏在横幅下方（不被遮挡）
✅ 页面背景正常显示
✅ Logo、菜单、按钮清晰可见
```

### 步骤 3：滚动页面
```
✅ 导航栏变成 sticky
✅ 导航栏在横幅下方（top: 40px）
✅ 导航栏显示毛玻璃效果
```

### 步骤 4：关闭横幅
```
✅ 横幅消失
✅ 导航栏回到顶部
✅ 页面布局正常
```

### 步骤 5：刷新页面
```
✅ 横幅重新出现
```

---

## 🔍 调试命令

### 检查 body padding
```javascript
// 在浏览器控制台（F12）运行
var body = document.body;
console.log('body padding-top:', window.getComputedStyle(body).paddingTop);
console.log('body 是否有 has-top-banner class:', body.classList.contains('has-top-banner'));

// 应该显示：
// body padding-top: 40px
// body 是否有 has-top-banner class: true
```

### 检查导航栏位置
```javascript
var header = document.querySelector('header');
var headerRect = header.getBoundingClientRect();

console.log('导航栏顶部:', headerRect.top);
console.log('导航栏是否在横幅下方:', headerRect.top >= 40);
```

### 关闭横幅后检查
```javascript
// 点击关闭按钮后运行
var body = document.body;
console.log('body padding-top:', window.getComputedStyle(body).paddingTop);
console.log('body 是否有 banner-closed class:', body.classList.contains('banner-closed'));

// 应该显示：
// body padding-top: 0px
// body 是否有 banner-closed class: true
```

---

## 📊 方案对比

### 方案 1：body padding（最简方案）✅
```css
body.has-top-banner {
    padding-top: 40px;
}
```
**优点：**
- 简单直接
- 所有内容一起移动
- 自动恢复

### 方案 2：header margin ❌
```css
body.has-top-banner header {
    margin-top: 40px;
}
```
**问题：**
- 横幅和导航栏之间出现间隙
- 需要负 margin 抵消

### 方案 3：header margin + content 负 margin ❌
```css
body.has-top-banner header {
    margin-top: 40px;
}
body.has-top-banner .site-content {
    margin-top: -40px;
    padding-top: 40px;
}
```
**问题：**
- 复杂，难以维护
- 导航栏可能被遮挡

---

## 🎯 关键点总结

### 布局策略
```
1. 横幅：position: fixed; top: 0; z-index: 999999
2. body：padding-top: 40px（所有内容向下移动）
3. sticky 导航栏：top: 40px（在横幅下方）
4. 关闭横幅：移除 has-top-banner class，padding 消失
```

### CSS 设置
```css
/* 桌面端 */
body.has-top-banner {
    padding-top: 40px !important;
}

/* 手机端 */
@media (max-width: 768px) {
    body.has-top-banner {
        padding-top: 56px !important;
    }
}

/* 关闭后 */
body.banner-closed {
    padding-top: 0 !important;
}
```

---

## ⚠️ 注意事项

### 1. Z-Index 层级
```
横幅：z-index: 999999（最上层）
导航栏：z-index: 99（中间层）
页面内容：z-index: auto（底层）
```

### 2. Sticky 导航栏
```css
/* 确保 sticky 导航栏在横幅下方 */
body.has-top-banner header.sticky {
    top: 40px !important;
}
```

### 3. 关闭横幅
```javascript
// JavaScript 需要做两件事：
// 1. 添加 banner-closed class 到 body
// 2. 移除 has-top-banner class 从 body
document.body.classList.remove('has-top-banner');
document.body.classList.add('banner-closed');
```

---

## 📋 修改记录

### 文件：functions.php

#### 简化（第 1379-1381 行）
```css
/* 之前：复杂的 margin 和负 margin */
body.has-top-banner header {
    margin-top: 40px !important;
}
body.has-top-banner .site-content {
    margin-top: -40px !important;
    padding-top: 40px !important;
}

/* 现在：简单的 body padding */
body.has-top-banner {
    padding-top: 40px !important;
}
```

#### 添加（第 1442-1444 行）
```css
/* 关闭横幅后，移除 body padding */
body.banner-closed {
    padding-top: 0 !important;
}
```

---

**修复时间**: 2025-12-10  
**状态**: ✅ 已完成  
**方案**: body padding（最简方案）

---

**请立即测试：**
1. ✅ 清除缓存（Ctrl+F5）
2. ✅ 访问英文首页
3. ✅ 检查导航栏是否在横幅下方（不被遮挡）
4. ✅ 滚动页面测试 sticky 导航栏
5. ✅ 点击关闭按钮测试
6. ✅ 刷新页面，横幅重新出现

**这是最简单、最可靠的方案！** 😊
